version: '3.9'
name: nestjs-homework

services:
  postgres: 
    image: postgres:15
    restart: always
    env_file:
      - .env
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - nestjs-homework

  redis:
    image: redis:6.2
    restart: always
    env_file:
      - .env
    ports:
      - 6379:6379
    volumes:
      - redis_data:/data
    networks:
      - nestjs-homework

  minio:
    image: minio/minio
    restart: always
    command: server /data --console-address ":9001"
    env_file:
      - .env
    ports:
      - 9000:9000
      - 9001:9001
    volumes:
      - minio_storage:/data
    networks:
      - nestjs-homework

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    restart: always
    ports:
      - 9092:9092     # для сервисов (внутри docker сети)
      - 9094:9094      # для подключения с хоста (локально)
    env_file:
      - .env
    volumes:
      - kafka_data:/var/lib/kafka/data
    networks:
      - nestjs-homework

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    restart: always
    ports:
      - 8080:8080
    env_file:
      - .env
    depends_on:
      - kafka
    networks:
      - nestjs-homework

  rabbitmq:
    image: rabbitmq:3-management
    restart: always
    ports: 
      - 5672:5672
      - 15672:15672
    env_file:
      - .env
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - nestjs-homework

  mailpit:
    image: axllent/mailpit
    restart: always
    ports:
      - 1025:1025   # SMTP
      - 8025:8025   # Web UI
    networks:
      - nestjs-homework

  mongodb:
    image: mongo:latest
    restart: always
    ports:
      - 27017:27017
    env_file:
      - .env
    volumes:
      - mongo_data:/data/db
    networks:
      - nestjs-homework

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
    env_file:
      - .env
    depends_on:
      - mongodb
    networks:
      - nestjs-homework


volumes:
  postgres_data:
  redis_data:
  minio_storage:
  kafka_data:
  rabbitmq_data:
  mongo_data:

networks:
  nestjs-homework:
    # external: true
      